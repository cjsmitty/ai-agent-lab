# Test-only dependencies — installed by Harness CI alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# Never installed into the container image.
pytest==9.1.1
httpx==0.28.1
